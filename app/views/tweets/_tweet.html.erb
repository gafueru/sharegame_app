<div class="card">
  <div class="card__body">
    タイトル：<%= link_to tweet.title, tweet_path(tweet.id), class: :card__title%>
  </div>
  <%= link_to image_tag(tweet.image, class: :card__img ), tweet_path(tweet.id) %>
</div>